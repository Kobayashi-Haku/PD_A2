# Renderでデータが消えてしまう問題の直し方（初心者向けガイド）

このアプリを Render にデプロイすると、しばらく使っていると登録した食品やユーザー情報が
**消えてしまう**ことがあります。これは「バグ」ではなく、Renderの仕組み上どうしても
起きてしまう問題です。この文書を読みながら設定すれば解決できます。

専門知識がなくても大丈夫なように、できるだけ難しい言葉を使わずに説明します。

---

## 1. なぜデータが消えるのか

このアプリは、今のままだと「H2」という、**アプリと同じ場所にファイルとしてデータを保存する
データベース**を使っています。

ところが Render の無料プラン（および多くの有料プランの通常のWebサービス）は、

- アプリを再起動したとき
- アプリを再デプロイ（更新）したとき
- 一定時間アクセスがなくて休止状態になり、また起動したとき

に、**保存していたファイルをすべて消してまっさらな状態に戻してしまいます**。

つまり、H2のようにファイルにデータを保存する方式は、Renderの環境とは相性が悪いのです。

これを解決するには、Renderが用意している「**消えない場所にデータを保存できるデータベース
（PostgreSQL）**」を使う必要があります。

---

## 2. Renderで無料のPostgreSQLデータベースを作る

1. ブラウザで [https://dashboard.render.com](https://dashboard.render.com) を開き、ログインします。
2. 画面右上あたりにある **「New +」**（新規作成）ボタンをクリックします。
3. メニューの中から **「PostgreSQL」** を選びます。
4. 次のような項目を入力する画面が出ます。
   - **Name（名前）**: 好きな名前でOKです（例: `food-manager-db`）。
   - **Database（データベース名）**: そのままで構いません。
   - **User（ユーザー名）**: そのままで構いません。
   - **Region（リージョン）**: アプリ本体（Webサービス）と同じリージョンを選ぶと安心です。
   - **Plan（プラン）**: 無料の **「Free」** を選びます。
5. 一番下の **「Create Database」** ボタンを押します。
6. 数十秒〜数分待つと、データベースが作成されます。

---

## 3. 接続情報（パスワードのようなもの）をコピーする

作成が終わると、データベースの詳細ページが表示されます。この中に、アプリと接続するための
情報があります。下にスクロールして、次の項目を探してください（呼び方は多少変わることがあります）。

- **Internal Database URL** または **External Database URL**
- **Username**
- **Password**
- **Hostname**
- **Port**
- **Database**

Renderの画面には「Internal Database URL」のような形で、
`postgres://ユーザー名:パスワード@ホスト名:ポート/データベース名`
という1行の文字列が表示されていることが多いです。これをそのままコピーしておいてください
（あとで少しだけ書き換えます）。

> **同じRenderアカウント内でWebサービスとデータベースの両方を動かす場合は
> 「Internal Database URL」を使うのがおすすめです（通信が速く、無料の範囲で済みます）。**

---

## 4. アプリ（Webサービス）側に環境変数を設定する

次に、このアプリ自体（Webサービス）の設定画面に移ります。

1. Renderダッシュボードで、このアプリのWebサービスを開きます。
2. 左側のメニューから **「Environment」**（環境変数）を選びます。
3. **「Add Environment Variable」**（環境変数を追加）をクリックして、以下の4つを **1つずつ** 追加します。

### (1) DATABASE_URL

先ほどコピーした接続情報を、次の形に**書き換えて**入力します。

Renderが見せてくれる形式（例）:
```
postgres://myuser:mypassword@dpg-xxxxxxxxxxxx-a/mydb
```

これをアプリ用に、先頭を `jdbc:postgresql://` に変え、ユーザー名とパスワードを
URLから外して、次のような形にします。

```
DATABASE_URL = jdbc:postgresql://dpg-xxxxxxxxxxxx-a/mydb
```

ポイント:
- 先頭が `postgres://` ではなく **`jdbc:postgresql://`** になっていることを確認してください。
- `ユーザー名:パスワード@` の部分は **DATABASE_URL には含めません**（ユーザー名・パスワードは下の(2)(3)で別に設定します）。

### (2) DATABASE_USERNAME

Renderの画面に表示されている **Username** をそのままコピーして入力します。

```
DATABASE_USERNAME = myuser
```

### (3) DATABASE_PASSWORD

Renderの画面に表示されている **Password** をそのままコピーして入力します。

```
DATABASE_PASSWORD = mypassword
```

### (4) HIBERNATE_DIALECT

これは固定の文字列です。そのまま入力してください。

```
HIBERNATE_DIALECT = org.hibernate.dialect.PostgreSQLDialect
```

---

## 5. 保存して再デプロイする

1. 4つの環境変数をすべて追加したら、画面の **「Save Changes」**（変更を保存）を押します。
2. Renderが自動的にアプリを再デプロイ（再起動）してくれます。少し待ちましょう。
3. デプロイが終わったら、アプリにアクセスして、新しく食品を登録してみてください。
4. 一度ブラウザを閉じて、しばらくしてからもう一度開いても、登録したデータが
   残っていればうまく設定できています。

---

## 6. 今までのH2のデータはどうなるの？

特に何もしなくて大丈夫です。

- 今まで使っていたH2のデータ（再起動すると消えてしまっていたデータ）を
  **新しいPostgreSQLに移す必要はありません**。
- 環境変数を設定した瞬間から、アプリは新しいPostgreSQLデータベースを使い始め、
  そこに新しく登録したデータがずっと保存されるようになります。
- つまり「ここから新しいデータベースで再スタートする」というだけです。

---

## 7. うまくいかないときは

- 環境変数の名前（`DATABASE_URL` など）が1文字でも間違っていないか確認してください。
- `DATABASE_URL` の先頭が `jdbc:postgresql://` になっているか確認してください
  （`postgres://` のままだと動きません）。
- Renderの「Logs」（ログ）画面を開くと、起動時にエラーが出ていないか確認できます。
  エラーメッセージが分からない場合は、そのままコピーして開発を手伝ってくれる人に
  見せると解決が早いです。
